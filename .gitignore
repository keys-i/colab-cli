/target
.env
.DS_Store

# generated by build.rs — credentials baked in CI before `cargo publish`
/crates/colab-cli/src/embedded.rs

# local network captures — may contain auth cookies
/high
/std
*.curl
