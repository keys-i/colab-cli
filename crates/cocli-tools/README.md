# cocli-tools

Typed built-in tool registry for `colab-cli`.

The crate exposes tool specs and JSON-serializable plan output for agent-friendly use. It does not run autonomous actions by itself; the CLI owns confirmation, credentials, and remote execution.
