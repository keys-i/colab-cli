# colab-cli

Public binary crate for the `colab-cli` workspace.

Most users install this crate:

```sh
cargo install colab-cli --locked
```

Run `colab-cli --help` for the command space. Workspace architecture and publish steps are documented in the repository root.
